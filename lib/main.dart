import 'package:flutter/material.dart';
import 'UI/Home.dart';

void main() => runApp(new MaterialApp(
      home: BillSplitter(),
      debugShowCheckedModeBanner: false, //to remove the debug tag on UI
    ));
